<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.vehicle_history.historiapojazdu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name="pl.vehicle_history.VehicleHistoryApp">

        <activity
            android:name="pl.vehicle_history.activity.MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="pl.vehicle_history.activity.VehicleDataActivity"
            android:label="@string/app_name"
            android:parentActivityName="pl.vehicle_history.activity.MainActivity" >

            <!-- Parent activity meta-data to support API level 7+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.vehicle_history.activity.MainActivity" />

        </activity>

    </application>

</manifest>
